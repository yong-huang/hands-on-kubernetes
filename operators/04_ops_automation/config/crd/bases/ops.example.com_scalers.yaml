---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: scalers.ops.example.com
spec:
  group: ops.example.com
  names:
    kind: Scaler
    listKind: ScalerList
    plural: scalers
    singular: scaler
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.targetName
      name: Target
      type: string
    - jsonPath: .status.lastReplicas
      name: Replicas
      type: integer
    - jsonPath: .status.currentSchedule
      name: Schedule
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: Scaler is the Schema for the scalers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Scaler
            properties:
              schedules:
                description: |-
                  schedules 是一组定时规则：当前时间匹配某条 cron 时，把目标调到对应 replicas。
                  多条规则可重叠（如工作日/节假日），取最后一条匹配的。
                items:
                  description: ScaleSchedule 一条定时扩缩容规则：cron 匹配时把目标 Deployment 调到 replicas
                  properties:
                    cron:
                      description: cron 表达式（分 时 日 月 周）
                      type: string
                    replicas:
                      description: cron 匹配时目标 Deployment 的期望副本数
                      format: int32
                      minimum: 0
                      type: integer
                  required:
                  - cron
                  - replicas
                  type: object
                minItems: 1
                type: array
              targetName:
                description: targetName 是要被扩缩容的目标 Deployment 名字（同 namespace）
                minLength: 1
                type: string
            required:
            - schedules
            - targetName
            type: object
          status:
            description: status defines the observed state of Scaler
            properties:
              conditions:
                description: conditions represent the current state of Scaler
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              currentSchedule:
                description: currentSchedule 是当前生效的 cron 规则
                type: string
              lastReplicas:
                description: lastReplicas 是 controller 最近一次设置的目标副本数
                format: int32
                type: integer
              lastScaleTime:
                description: lastScaleTime 是最近一次执行扩缩容的时间
                format: date-time
                type: string
              observedGeneration:
                description: observedGeneration 记录 controller 最近处理的 generation
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
