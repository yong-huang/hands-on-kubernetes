apiVersion: platform.example.com/v1
kind: MicroService
metadata:
  name: user-service
spec:
  image: "nginx:alpine"
  replicas: 2
  port: 80
  env:
    LOG_LEVEL: "info"
    DB_HOST: "postgres.internal"
  configData:
    nginx.conf: "server { listen 80; }"
  ingressHost: "user-service.example.com"
